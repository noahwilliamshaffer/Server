# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/ee/user/application_security/secret_detection/#customizing-settings
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence
image: "python:latest"

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - Static Analysis
  - Test

mypy:
  stage: Static Analysis
  script:
  - pwd
  - ls -l
  - python -m mypy ProjectFiles/myproject.py

flake8:
  stage: Static Analysis
  script:
  - flake8 --max-line-length=120 ProjectFiles/myproject.py
  # it will be all .py files once its cleaned up.

pylint:
  stage: Static Analysis
  allow_failure: true
  script:
   - pylint -d C0301 ProjectFiles/myproject.py
  # it will be all .py files once its cleaned up.
# the unit test report
pytest:
  stage: test
  script:
    - pytest --junitxml=report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml

connectionTest:
  stage: Test
  script:
    - curl - v https://www.noahwilliamshaffer.com

sast:
  stage: Test
include:
- template: Security/SAST.gitlab-ci.yml
